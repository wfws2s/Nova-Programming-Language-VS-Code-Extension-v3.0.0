# Runtime

The runtime is implemented and provides NOVA values, lexical environments, built-ins, collection mutation, and controlled runtime errors.

Current built-ins are `print`, `input`, `len`, `number`, and `string`. Planned Full-release APIs such as `read`, `split`, `list`, and `typeof` are specified in `docs/TYPES.md` and have not yet been added to this runtime.
