# NOVA Demo: Functions, Recursion, and Loops

fn fib(n)
    if n <= 1
        return n
    end
    return fib(n - 1) + fib(n - 2)
end

print("Calculating Fibonacci numbers:")
for i in 0..10
    print("fib(" + string(i) + ") =", fib(i))
end
